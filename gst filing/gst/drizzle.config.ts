import { defineConfig } from "drizzle-kit";import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
if (!process.env.DATABASE_URL) {import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
  throw new Error("DATABASE_URL, ensure the database is provisioned");import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
}import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
export default defineConfig({import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
  out: "./migrations",import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
  schema: "./shared/schema.ts",import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
  dialect: "mysql",import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
  dbCredentials: {import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
    url: process.env.DATABASE_URL,import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
  },import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
});import { defineConfig } from "drizzle-kit";
import dotenv from "dotenv";

dotenv.config();

export default defineConfig({
  dialect: "mysql",
  schema: "./shared/schema.ts",
  out: "./migrations",
  dbCredentials: {
    host: "localhost",
    user: "root",
    password: "vavi@1213",
    database: "gst_filing_db",
    port: 3306
  },
  verbose: true,
  strict: true
});
